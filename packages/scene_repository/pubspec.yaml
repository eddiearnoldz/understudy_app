name: scene_repository
description: "The repo that handles scenes"
publish_to: 'none' 
version: 0.0.1

environment:
  sdk: '>=3.4.0-190.0.dev <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  firebase_auth: ^4.17.6
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

